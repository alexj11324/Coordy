# Coordy 能力清单

对照公开工作区产品文档与 Coordy 当前桌面/内核。实现只走 `submit` / `view` / `watch`；缺字段就扩展 `crates/coordy-protocol`，不接入外部产品协议。

状态：`[x]` 本机已有可用路径（可以比对照产品窄）；`[ ]` 未做，其中云通道与甘特等明确不假装实现。

---

## 桌面壳

- [x] 可收起侧栏
- [x] 顶栏可关闭标签页
- [x] `C` 新建任务、`Mod+K` 搜索
- [x] 侧栏结构：收件箱 / 聊天 / 我的任务；工作区（任务、项目、自动化、智能体、小队、统计）；配置（Harness、Skills、设置）
- [x] 工作区切换器（名称 + 图标 + 下拉）
- [x] 搜索框外观（占位「搜索…」+ 快捷键）
- [x] 「新建任务」按钮上显示 `C`
- [x] 右下角悬浮聊天入口（停止 / 归档）
- [ ] 标签页：图标、固定、拖拽排序、前进/后退、按工作区分组
- [ ] 窄窗口时标签避开红绿灯；侧栏收起后顶栏仍能展开
- [ ] 可自定义快捷键（设置 → 快捷键录制/冲突/重置）

## 工作区与账号

- [x] 工作区名称、图标、描述、智能体背景上下文
- [ ] 工作区 slug（协议有字段，设置页尚未提供）
- [x] 递增任务编号前缀（启动时前置 `ws.context`）
- [x] 多工作区创建 / 切换 / 离开 / 删除
- [x] 成员角色：owner / admin / member；邀请
- [x] 账号：资料、偏好、通知种类开关
- [ ] 桌面系统通知
- [ ] 云账号 / 个人访问令牌（本机不提供）

## Issues / 任务

- [x] 标题、说明、状态、指派智能体、看板 + 列表、拖拽改状态
- [x] 创建事项：优先级 / 截止日期 / 标签 / 项目 / 附件 / 启动智能体
- [x] 事项详情：评论时间线与执行记录分开
- [x] 评论走 `AddComment`；普通发送不再伪装成评论
- [x] `@智能体` 走 `StartMentionRun`，不改负责人
- [x] 让负责人继续执行走 `StartRun`
- [x] 失败/取消后 `RetryRun` 再次 spawn，沿用原 prompt
- [x] 可改优先级 / 标签 / 日期 / 项目 / 成员或小队指派
- [x] 子事项（`parent_id`）；可选「建议拆分」（需已配置模型密钥）
- [ ] 对话式任务拆分闭环（Trellis：`08-19-conversation-task-decomposition`）
  - [ ] 智能体根据对话生成结构化父任务与子任务草案，而不直接修改任务状态
  - [ ] 草案包含说明、验收标准、优先级、阶段、依赖、建议智能体或小队
  - [ ] 在聊天中预览、编辑、删除、重新生成拆分结果
  - [ ] 用户一次确认后由 kernel 原子化批量创建；任一校验失败则全部不创建
  - [ ] 自动建立父子关系、无环依赖和并行/串行阶段
  - [ ] 确认并开始时只派发当前阶段且依赖已满足的子任务
  - [ ] 父任务展示子任务进度；满足完成规则后自动完成
  - [ ] 内建规划 Skill 指导 agent 生成计划；权限、校验、提交和调度仍由 kernel 保证
- [x] 附件列表与删除；删除事项走 `DeleteTask`
- [x] 状态含 `backlog`；待规划事项不入队
- [ ] 自定义属性读写
- [ ] 表格视图、甘特图、泳道
- [ ] Markdown 描述编辑器
- [ ] 评论线程、标记已解决、设为结论、表情回应
- [ ] 订阅 / 取消订阅一条 issue
- [ ] 看板拖拽排序（不只改状态）

## 评论与收件箱

- [x] Issue 评论独立于执行
- [x] `@智能体` 触发执行且不改负责人
- [x] 收件箱已读 / 归档 / 忽略
- [x] 设置里按类型开关通知
- [ ] `@成员` 桌面通知、链接其他 issue
- [ ] 收件箱覆盖分配、提及、评论、状态变化等全部种类的产品级聚合

## 聊天

- [x] 侧栏「聊天」：与智能体一对一、不依附 issue
- [x] 右下角悬浮窗：`CreateChat` / `SendChatMessage`
- [x] 停止当前 run、归档会话
- [ ] 聊天附件 / 图片
- [ ] 可挂项目上下文（内核字段已有，桌面未做选择器）

## 智能体

- [x] 列表只显示已创建身份；新建（空白 / 对话起草）
- [x] 名称、描述、指令、harness、模型、思考强度、Codex Fast
- [x] DiceBear 头像；编辑；归档；名称工作区唯一
- [x] Access：仅自己 / 整个工作区 / 指定成员
- [x] 工具权限：Auto / Full Access（按 harness 下发不同启动参数）
- [x] 绑定 Skills（`SetAgentSkills`，下一次 `StartRun` 注入正文）
- [x] 并发上限（running 超额则拒绝 `StartRun`）
- [x] `cli_args` 追加到原生启动参数（ACP 忽略）
- [x] 复制智能体
- [x] Agent Builder：有模型密钥则对话调用 LLM 填草稿；无密钥明确失败
- [ ] 环境变量、把 MCP 写入各家 CLI 配置文件

## 运行时与执行

- [x] 本机发现 Claude Code / Codex / Gemini 等；在线/离线；刷新
- [x] 密钥本机 0600；原生 spawn
- [x] Daemon 绿灯时 `RegisterComputer` 登记当前主机
- [x] 设置 Daemon 页展示已登记电脑
- [x] 智能体并发上限
- [x] 无人值守 auto-approve：按 harness 注入 skip/yolo/sandbox，ACP 自动放行权限请求
- [x] 智能体工具权限 Auto / Full Access：Auto 仍拦危险步骤，Full Access 才跳过审批
- [ ] 执行队列状态机的完整产品页
- [ ] 云端 / 远程运行时
- [ ] 应用商店更新通道

## Skills

- [x] 工作区 Skills 库：名称 + 正文
- [x] 挂到智能体；按智能体多选绑定
- [ ] 从 URL 导入、从本机运行时复制 `SKILL.md` 附件

## 项目 / 自动化 / 小队 / 统计

- [x] 项目：说明、状态、lead、日期、资源；列出子事项
- [x] 自动化：runbook、指派智能体、`create_issue`、手动立即运行
- [x] 本机间隔调度（`every:30m` / `every:1h` / `every:1d`），coordyd 轮询 `TriggerAutomation`
- [x] 小队：领队 + 成员；事项指派小队后对领队 `StartRun`
- [x] 统计页走 `Query::Stats`
- [ ] 公网 Webhook 入站
- [ ] 甘特图 / 泳道

## 设置

- [x] 我的账号：资料、偏好、快捷键列表、任务/聊天说明、通知、模型密钥、Daemon、更新说明
- [x] 工作区常规：名称、描述、上下文、slug、任务前缀、离开/删除
- [x] 代码仓库（绑定本机目录）
- [x] 云通道：一条说明，本机不提供 IM 云通道（不造 OAuth 表单）
- [x] 成员、标签、实验室顾问开关
- [x] MCP 页：Coordy 不托管 MCP，各 CLI 读自己的配置
- [x] GitHub：本机 `gh` 同步 PR / CI，事项侧栏展示状态、检查与可合并性
- [ ] GitHub App / 公网 webhook / OAuth 安装
- [ ] 飞书 / Slack / 钉钉 / 企业微信
- [ ] 自定义属性编辑入口
- [ ] 快捷键录制 / 冲突 / 重置

## CLI 与集成（本机不做）

- [ ] 产品级 CLI 覆盖 issue / project / agent / skill / squad / automation / runtime
- [ ] GitHub App：按编号关联 PR、合并关 issue（本机 `gh` 路径已覆盖只读同步与关闭语句转 Done）
- [ ] 聊天工具接入（飞书 / Slack / 钉钉 / 企业微信）
- [ ] 公网 Webhook 监听

## 界面质感

- [x] 侧栏密度、分组、搜索条、快捷键徽章
- [x] 设置页：左侧设置导航 + 表单
- [ ] 标签页图标、固定与拖拽

内核另有记忆、契约、授权委托、依赖边、compaction 冲突等对象。它们不是对照缺口；不要从主侧栏冒充外部产品导航。
